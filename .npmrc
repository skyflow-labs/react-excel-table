# GitHub Packages registry for @skyflow-labs scope
@skyflow-labs:registry=https://npm.pkg.github.com

# Authentication token (use environment variable, never commit actual tokens)
//npm.pkg.github.com/:_authToken=${NPM_TOKEN}
